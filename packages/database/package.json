{
  "name": "@durable/database",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "prisma generate && tsc --build",
    "test": "vitest run",
    "db:migrate": "dotenv -e ../../.env.test -- prisma migrate dev"
  },
  "dependencies": {
    "@prisma/client": "^6.4.1"
  },
  "devDependencies": {
    "dotenv-cli": "^8.0.0",
    "prisma": "^6.4.1",
    "typescript": "^5.8.2",
    "vitest": "^3.0.7"
  }
}
